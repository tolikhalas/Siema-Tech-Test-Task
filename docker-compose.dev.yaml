version: '3.8'

services:
  backend:
    build:
      dockerfile: Dockerfile.dev
    environment:
      NODE_ENV: development
    volumes:
      - .:/app
    env_file:
      - .env.development.local

  db:
    env_file:
      - .env.development.local
