import { AssignPermissionsDto } from "../assign-permissions.dto";

describe("AssignPermissionsDto", () => {
  it("should create an AssignPermissionsDto instance", () => {
    const dto = new AssignPermissionsDto();
    expect(dto).toBeDefined();
  });
});
